# AGENTS.md — Receps-IA (runtime WhatsApp compartilhado)

Runtime compartilhado via WhatsApp Cloud API que hospeda duas personas: **Ana**, recepcionista das clínicas, e **Renata**, vendas/onboarding da Receps. Prod: VPS `root@46.62.134.25` em `~/Receps-IA`, processo PM2 **receps-ia** (porta **3001**). O Receps roda na MESMA VPS em `localhost:3000`. Auth Receps-IA→Receps: `Authorization: Bearer <ERP_API_TOKEN>` (= `AI_BOT_API_KEY` do Receps; ver `src/erpApiToken.ts`).

**Fronteira de nomes:** Receps-IA é projeto/repo/processo/runtime. Ana e Renata continuam sendo as personagens; não renomeie prompts, tools, `botRole`, modelos/tabelas `Ana*`, flags funcionais `ANA_*` nem assets `RENATA_*` por causa da identidade técnica.

## Conexão de novos tenants — Embedded Signup público (resolvido 2026-07-30)

- Cada cliente conecta o próprio WhatsApp Business no Receps pelo Embedded Signup/Coexistence; a Ana apenas consome o `BotConfig` resultante. Não existe etapa normal de ativação manual, convite como tester ou acesso do Victor ao Business do cliente.
- Meta App `1248305527427779`; configuração v4 ativa em produção `1728530234932540` (`Receps Embedded Signup v4`). Permissões usadas: `whatsapp_business_management` + `whatsapp_business_messaging`; `business_management` não é necessária.
- A falha que bloqueava contas sem função no app era `public_profile` em “Pronto para teste”. Aumentar o acesso para “Pronto para publicar” resolveu sem novo App Review; o fluxo real foi confirmado com conta comum e tenant de testes.
- O Receps mantém `META_EMBEDDED_SIGNUP_ENABLED` somente como kill-switch operacional. Não reintroduzir na documentação nem no produto o fluxo histórico anterior à liberação pública.

## Deploy (POR GIT — a seção "rsync" antiga está morta)
O repo é `NieXFS/Receps-IA`, checkout de `origin/main` em `/root/Receps-IA`. **⚠️ NUNCA editar `/root/Receps-IA` direto na VPS** — em 2026-07-15 um hot-patch de 438 linhas no checkout antigo travou o `git pull`.

No **primeiro cutover** a identidade antiga ainda é `ana-bot` em `/root/Ana`: prepare e builde `/root/Receps-IA` sem iniciar uma segunda cópia, então execute `pm2 stop ana-bot` → `pm2 start ecosystem.config.cjs --only receps-ia` → health/listener → `pm2 delete ana-bot` → `pm2 save`. Não use `restart receps-ia` antes desse bootstrap, e não mantenha os dois runtimes ativos porque ambos iniciam pollers.

Nos deploys seguintes, com `receps-ia` já criado pelo ecosystem:
```
ssh root@46.62.134.25 "cd ~/Receps-IA && git pull && npm install --no-audit --no-fund && npm run build && pm2 startOrReload ecosystem.config.cjs --only receps-ia --update-env"
```
- `npm install` só é dispensável se o `package.json` NÃO mudou (armadilha do `xlsx`/`@anthropic-ai/sdk`).
- `npm run build` é só `tsc` (saída em `dist/`, `start` roda `dist/webhookServer.js`). **Confira o exit REAL e o `dist/` emitido** antes do restart.
- **O reload/restart do PM2 não é opcional**: em 2026-07-20 um deploy fez pull+build sem restart e o processo rodou 1,5 dia com código velho enquanto o prompt (banco, hot-reload) já prometia features novas — a "assimetria fatal". Confira que o processo subiu DEPOIS do build.

## Assets binários
`assets/renata-demo.mp4` (10,7MB) é o vídeo da escada de demonstração da Renata, **commitado de propósito**: o `video/` do repo Receps é gitignored, então o `git pull` é o único caminho que leva o arquivo pra VPS. O `boot()` não lê o asset; ele só é carregado no 1º `sendDemoVideo` (e aí fica ~11MB memoizado no processo). Env opcional `RENATA_DEMO_VIDEO_PATH` sobrepõe o caminho.

## Smokes (determinísticos, sem rede/OpenAI/DB)
Rodados com `npx tsx`. Padrão pós-ESM: setar `process.env.DATABASE_URL`/`OPENAI_API_KEY` dummy ANTES de carregar o módulo (o `contextManager` LANÇA no load sem `DATABASE_URL`; o `Pool` do pg é lazy, não conecta), usar `import type` pros tipos e `await import()` dinâmico pro módulo real. Lista em `package.json` (`smoke:*`).

## Multi-provider: Ana recepcionista e Renata vendas (2026-08-01)

- O brain da recepcionista aceita somente `aiProvider:"openai"` com `gpt-4o-mini` (rollback explícito) ou `aiProvider:"deepseek"` com `deepseek-v4-flash` (default de novos `BotConfig` desde 2026-08-03). O mesmo default novo ativa atendimento 24 horas; ele não reescreve configurações existentes. A Renata (`botRole:"sales"`) tem registry e painel próprios, sem fallback cruzado, e fica fora deste escopo; o estado produtivo dela deve ser revalidado no runtime antes de qualquer mudança.
- DeepSeek usa `DEEPSEEK_API_KEY`, base fixa `https://api.deepseek.com` e thinking explicitamente `disabled`. Nunca faz fallback cruzado; provider/model/chave incompatíveis falham fechado. **A chave sozinha NÃO autoriza dados reais:** em `NODE_ENV=production`, a resolução do provider e a montagem do request exigem também `DEEPSEEK_PRODUCTION_APPROVED=true`. A flag é um gate formal do operador e não seleciona provider nem altera tenants; o default da flag continua `false`. Desde 2026-07-30, DeepSeek e o tratamento na China constam da lista pública de subprocessadores e o operador autorizou o canário da recepcionista para `centro-estetico-jackeline-hussar` e `studio-viti`. Em 2026-08-03, o rollout controlado migrou também `rose-pacheco-podologia`; a reauditoria deixou os três em DeepSeek e manteve `receps-admin` em OpenAI. Qualquer expansão futura continua exigindo dry-run, autorização operacional e `--apply` explícito. A consulta autenticada de `/models` em 2026-08-02 expôs só os aliases `deepseek-v4-flash` e `deepseek-v4-pro`, sem ID versionado pinável; o registry fechado usa o alias aceito e o runtime registra `requested_model` e `response_model`, sem fingir que isso congela a versão subjacente. Para a Renata, resultados anteriores de críticos vieram de snapshots/configurações diferentes e **não** formam um 50/50 homogêneo: o gate exige as repetições frescas no mesmo harness/prompt/config e autorização explícita para qualquer canário.
- O loop compartilhado `runReceptionistModelLoop` não tem I/O próprio. Produção injeta as tools reais; `scripts/benchmarks/ana-models/runner.ts` injeta somente fixtures `dryRun` em memória.
- O benchmark sobrescreve URLs de DB/ERP com destinos locais inválidos antes de importar o brain; não acessa Postgres, Receps, WhatsApp, booking ou cancelamento reais. Protocolo e comandos em `docs/benchmark-ana-models.md`; outputs em `benchmark-results/` (gitignored).
- Hard blocks novos: `bookingConfirmationGate` impede `bookAppointment` sem resumo anterior + confirmação inequívoca (frases como "acho que pode" não autorizam escrita); `cancellationIntentGate` + `resolveCancellationTarget` só cancelam no fluxo de duplicidade e nunca escolhem entre 2+ agendamentos sem referência atual inequívoca; `customerReplyGuard` impede `INTERNAL_HINT`/IDs técnicos e afirmações falsas de escrita de chegarem ao WhatsApp, licencia descrições de estado somente por leitura autoritativa compatível e normaliza Markdown/listas/excesso de emoji antes do envio. Desde 2026-08-03, uma oferta concreta de horários exige fonte autoritativa **do turno atual** contendo todos os slots citados: `getAvailableSlots success:true` com `slots`, ou `bookAppointment success:false` com reason exatamente `blocked`/`conflict`/`outside_hours` e `availableSlots`. No segundo caso o `calendarService` já consultou internamente: só os valores exatos de `availableSlots` podem ser ofertados e não se chama `getAvailableSlots` de novo; lista vazia não tem alternativa naquele dia. `message`/`hint`, reason `package_exhausted`/`other`, sucesso de book, array ausente/inválido, divergência ou turno antigo geram `unverified_availability` e fazem fallback fail-closed. Negativa de disponibilidade, duração, preço, horário de funcionamento e perguntas não entram nessa regra. Desde 2026-08-04, o **Guardrail D** (`professional-selection-gate.ts`) roda depois do gate de serviço e antes de toda I/O de calendário: usa `professionalIds ∩ profissionais ativos` (`undefined` = fallback legado global; `[]` = nenhum), bloqueia 0/1/2+ conforme a escolha mais recente do CLIENTE, e nunca licencia preferência pelo texto do assistant. Prefixo globalmente unívoco é canonicalizado só para a I/O; a trace preserva o argumento bruto do modelo. O benchmark/reauditoria registra o motivo `professional_selection`; essa proteção não converte tentativa bruta errada em aderência do LLM.
- `getUpcomingAppointments` é read-only e recupera os IDs autoritativos no novo turno da decisão de duplicidade. O gate de serviço preserva confirmações como "sim, pode marcar"; no book de remarcação só libera o serviço já proposto depois de cancelamento bem-sucedido + `confirmedDuplicate:true`.
- Smokes: `smoke:receptionist-provider`, `smoke:receptionist-prompt-guards`, `smoke:booking-confirmation-gate`, `smoke:customer-reply-guard`, `smoke:cancel-appointment-guard`, `smoke:service-gate`, `smoke:professional-selection-gate`.

## PII / scrub
NUNCA logar PII em claro nem colar telefone/nome/mensagem crus. `messageId`/`wamid` cru também é sensível: pode carregar o telefone do remetente de forma reversível; use apenas `messageIdHash`. O Sentry tem scrub (`src/observability/scrub.ts`) por chave E em strings livres (`event.message`, exceptions, breadcrumbs). Ao acompanhar `pm2 logs receps-ia`, redija telefone/nome ao reportar.

## Contrato internacional Ana → Receps (W1, 2026-08-10)
`customerPhone` deve sair da Ana no fio como E.164 explícito (`+<wa_id>`) no outbox de inbound e na escalada. O Receps mantém `+55…` como canônico brasileiro e só-dígitos para DDI estrangeiro; cadastro/login do produto continuam BR-only. Um 4xx do contrato põe o inbound em quarentena terminal: depois de corrigir e publicar Receps + Ana, rearme o item exato pelo endpoint autenticado `/internal/inbound-outbox/reprocess`; o sweep comum não o recupera sozinho. Smokes obrigatórios do contrato: `smoke:ana-outbox`, `smoke:ana-outbox-quarantine`, `smoke:scrub-message` e, no Receps, `smoke:ana-inbound-contract`.

## Fixes registrados

### FIX 1 — Saudação dobrada no 1º contato (`src/services/brainService.ts`)
A **saudação de boas-vindas é da PRÓPRIA Ana** (não do ERP): no 1º contato, `maybePrependGreeting` colava `config.greetingMessage` ("Olá! Sou a Ana... Como posso te ajudar hoje?") na frente da resposta, mas o modelo TAMBÉM saudava → "Como posso ajudar" duplicado. O guard antigo (`reply.includes(greeting)`) só pegava match exato da string inteira. Agora há `replyAlreadyGreets(reply)` (PURO, exportado): normaliza os ~80 primeiros chars (lowercase, sem acento) e retorna true se baterem padrões de saudação (`ola`/`oi`/`bom dia`/`boa tarde`/`boa noite`, com fronteira de palavra) OU `como posso ... ajudar`. `maybePrependGreeting` pula o prepend quando `replyAlreadyGreets` é true; continua prependando quando a resposta NÃO saúda (ex.: cliente pergunta preço direto). Smoke: `smoke:greeting-prepend`.

### FIX 2 — Aviso de fora-de-horário sem throttle / spam (`src/messageHandler.ts`)
O **aviso de fora-de-horário também é da PRÓPRIA Ana** (`buildOutsideHoursMessage`, montado a partir de `botActiveStart`/`botActiveEnd`). Quando `isBotActive` é false, o handler mandava o aviso 1x POR mensagem recebida (sem dedup) → 3 mensagens iguais seguidas no teste real. Agora há `shouldSendOutsideHoursNotice(bufferKey, now?)` (PURO, exportado) com um `Map` "suprimido até" (mesmo padrão do `flushRecoveryUntil`), janela `OUTSIDE_HOURS_NOTICE_WINDOW_MS = 4h`. Só envia o aviso se a função autorizar (e ela marca o timestamp ao autorizar). Chave = `bufferKey` (`phoneNumberId:from`). O `Map` é limpo em `__resetFlushStateForTest`. Smoke: `smoke:outside-hours-throttle`.

### FIX 3 — Elegibilidade profissional↔serviço (`calendarService.ts` + `brainService.ts`)
**Fonte de elegibilidade por serviço = `services[].professionalIds` do ERP** (`/api/v1/agenda/info`): array de ids dos profissionais habilitados PRA CADA serviço. A Ana consome isso pra só ofertar/agendar com profissional que atende o serviço escolhido. **Tolerante ao campo ausente** (ERP antigo): sem ele, mantém o comportamento antigo (lista global).
- `calendarService.ts`: `ErpService.professionalIds?: (string|number)[]`; `ServiceSummary.professionalIds?: string[]`; `normalizeServices` mapeia pra `string[]` SÓ quando presente (senão `undefined`). Tipos `ServiceSummary`/`ProfessionalSummary`/`ServicesResult` exportados. Defesa em profundidade: no catch de `getAvailableSlots`, erro axios 400 cuja msg case com `/não pode realizar|não está ativo/i` → `INTERNAL_HINT` específico (profissional não atende o serviço; oferecer outro habilitado), nunca repassado ao cliente.
- `brainService.ts`: `buildServicesBlock(servicesResult)` (PURO, exportado) monta o bloco de serviços do system prompt. Modo elegibilidade (quando há `professionalIds`): lista "Profissionais habilitados" POR serviço (interseção com a lista global ativa) com nome + id; interseção vazia → marca "NENHUM no momento" (não ofertar/agendar); NÃO repete o bloco global "PROFISSIONAIS DISPONÍVEIS" (economia de tokens). Fallback (sem `professionalIds`): formato antigo (lista global + bloco "PROFISSIONAIS DISPONÍVEIS"). Regra de fluxo **C (ESCOLHA DO PROFISSIONAL)** reescrita pra usar o set POR SERVIÇO: 0 habilitados → avisa sem profissional e oferece outro serviço; 1 → agenda direto sem perguntar; 2+ sem escolha → pergunta "específico ou tanto faz?" ("tanto faz" → bookAppointment SEM professionalId, auto-resolve). Smoke: `smoke:services-block-eligibility`.

#### Marcador de "profissional único" (adesão do mini à regra C(b))
No self-test pós-FIX 3, o gpt-4o-mini ignorava a regra C(b) ~1 em 4: perguntava preferência de profissional MESMO quando o serviço tinha só 1 habilitado. Os dados/filtragem estavam certos — era adesão do modelo. Fix: em `buildServicesBlock` (modo elegibilidade), quando a interseção (habilitados ∩ global ativa) tem **exatamente 1**, a linha do serviço vira um marcador IMPERATIVO inline em vez do rótulo plural: `Profissional único habilitado: <Nome> — id: <id>. Agende DIRETO com ele(a); NÃO pergunte preferência de profissional.` Instrução colada na própria linha do serviço cola muito melhor no mini do que a regra C genérica lá embaixo (que segue intacta, como backup). 2+ habilitados → formato plural normal; 0 → aviso "NENHUM no momento". Smoke cobre os 3 ramos (1/2+/0). Resultado medido (studio-viti, 1 habilitado, self-test de 20 turns "quero Peeling"): perguntas de preferência caíram de ~1/4 pra ~1/20 (5%).

#### Guardrail D — seleção profissional contextual (2026-08-04)
`src/services/professional-selection-gate.ts` é PURO e é a defesa funcional para `getAvailableSlots` e `bookAppointment`; recebe o snapshot `ServicesResult` já carregado e as mensagens do **usuário** da intenção recente, sem calendário/ERP/DB. Fonte de elegibilidade = `service.professionalIds` intersectado com `professionals` ativos: `professionalIds: []` bloqueia e orienta indisponibilidade; `undefined` preserva o fallback global para ERP legado/misto. Com 1 habilitado, ausência, prefixo ambíguo ou outro ID recebem `INTERNAL_HINT` para refazer imediatamente com o ID correto; prefixo globalmente unívoco vira o ID canônico só na I/O. Com 2+, sem escolha bloqueia o prefetch e manda perguntar “Profissional específico ou tanto faz?”; “tanto faz/qualquer um/sem preferência” exige **ausência** de `professionalId`; nome explícito exige o ID daquele profissional. Nome completo casa sempre; primeiro nome/token só casa quando é unívoco na lista ativa, portanto homônimos continuam fail-closed. Correção de data preserva uma escolha anterior, mas novo agendamento ou mudança de serviço abre/reavalia a intenção (ex.: Peeling/Marina → Limpeza/Júlia). `scripts/benchmarks/ana-models/runner.ts` e `--reaudit` usam o mesmo gate, contam `professional_selection` e não reescrevem `entry.args`; isso é proteção de runtime, não melhora retroativamente a aderência bruta medida em 2026-08-04. Smoke: `npm run smoke:professional-selection-gate`.

“Mudei de ideia”/“agora quero” sem um serviço atual nomeado **não** reiniciam a preferência — são compatíveis com correção de data. Uma troca inferida exige evidência de serviço diferente no texto do cliente, ou as formas explícitas “outro/novo/trocar o serviço”. “Tanto faz”, “qualquer” e “sem preferência” só autorizam autoatribuição em resposta curta/autônoma (cortesia simples permitida) ou em formas profissionais explícitas como “qualquer profissional” e “quem estiver disponível”; termos qualificados por horário/data/dia/turno não escolhem profissional. Exclusão/negação como “qualquer um menos Marina” permanece ambígua e bloqueada.

O pronome “qualquer um/uma” também só é profissional quando for resposta curta autônoma: “qualquer um dos horários”, “qualquer uma das datas” e “qualquer um desses horários” são temporais e falham fechados. Nome em rejeição/exclusão (“não quero a Júlia”, “menos a Júlia”) nunca é escolha positiva. “Outra profissional”, “profissional diferente” e “trocar/mudar de profissional” limpam a preferência anterior e bloqueiam até uma escolha posterior inequívoca — por exemplo, “com a Marina Costa”.

> Contraparte no Receps: `getAgendaInfoForBot` (`src/services/agenda-api.service.ts`) expõe `services[].professionalIds` de forma aditiva. Ver AGENTS.md do Receps.

### FIX 4 — Pausa da Ana (echo humano + checagem de pausa) (2026-06-22)
A Ana **fica calada** quando a CONVERSA ou o SALÃO estão pausados, e **se pausa sozinha** quando o humano responde pelo app do WhatsApp (echo). O **Receps é a fonte da verdade** da pausa; a Ana só consome 2 endpoints (auth `Bearer ERP_API_TOKEN`, base `RECEPS_INTERNAL_API_URL`, padrão do `optOutService`). **Mudança ADITIVA e FAIL-OPEN**: se a API de pausa falhar/timeout/404, a Ana age como antes (responde normal). **Nunca loga PII** (só `phoneNumberId` nas tags Sentry, nunca número/nome do cliente).

- **Endpoints consumidos** (`src/services/pauseService.ts`):
  - `GET /api/v1/bot/pause-state?phoneNumberId=&customerPhone=` → `{ globalPausedUntil, conversationPausedUntil }` (ISO|null; passado/null = não pausado).
  - `POST /api/v1/bot/pause-conversation { phoneNumberId, customerPhone, source: "echo" }` → `{ pausedUntil }` (o ERP aplica o `echoPauseMinutes` do tenant).
- **Echo (`smb_message_echoes`, Coexistence)** — `src/echoHandler.ts` + branch novo no `src/webhookServer.ts`: o POST `/webhook` agora ramifica por `change.field`. `field === "smb_message_echoes"` → `handleSmbMessageEchoes` (NÃO vai pro `handleIncomingMessage` — não é mensagem de cliente); senão segue o caminho `value.messages` de sempre. Parser PURO `parseEchoTargets(value, fallbackPhoneNumberId?)`: shape da Meta = `value.metadata.phone_number_id` + `value.message_echoes[]` com `from`(salão)/`to`(**cliente**) → o número a pausar é o **`to`**. Como a Ana envia pelo Cloud API (não pelo app), **todo** echo = ação humana → pausa (não filtra por type; o mais simples/seguro). Defensivo: ignora payload sem `phone_number_id`/`to`; deduplica por cliente. Pra cada alvo chama `pauseConversationByEcho` (POST acima) e escreve no cache local **antes do primeiro await**.
- **Checagem antes de responder** — `src/messageHandler.ts`, em `handleIncomingMessage` DEPOIS do `tryHandleOptOut` (opt-out/compliance vale mesmo pausado) e ANTES do `conversationTracker.markActive`/buffer: `if (await isConversationPaused(config.phoneNumberId, from)) { await recordInboundWhilePaused(...); return; }` (sem OpenAI, sem buffer; log sem PII). O `flushBuffer` também revalida a pausa **antes do brain e antes do envio**: cobre inbound que já estava nos 12s de debounce e echo que chega enquanto o LLM está em voo; em ambos os casos preserva textos ainda não processados no histórico e não envia resposta/fallback. **⚠️ Atualizado no FIX 5 (2026-06-28):** enquanto pausada a Ana agora **GRAVA** o inbound do cliente no histórico (e o echo do humano também é gravado, como `assistant` com prefixo `[atendente] `) pra ter contexto ao retomar — antes descartava. Ver FIX 5.
- **Cache** (`pauseService`): por conversa (`phoneNumberId:customerPhone`, mesma forma do `bufferKey`). TTL curto do GET (`PAUSE_STATE_TTL_MS = 25s`) → 1 GET cobre uma rajada e reflete pausas do painel em ~25s. O echo faz **write-through otimista imediato ANTES do POST** (Ana respeita a pausa sem abrir a janela de timeout da rede); a resposta do Receps substitui o carimbo pelo `pausedUntil` autoritativo. Se o POST falhar, mantém a pausa local otimista (`ECHO_LOCAL_FALLBACK_MS = 60min`) pra não falar por cima do humano. Decisão PURA isolada em `src/services/pauseDecision.ts` (`isActivePause`/`isPausedFromState`).
- **Smokes**: `smoke:pause-decision` (helper puro: futuro=pausado, passado/null/inválido=ativo, global vs conversa), `smoke:echo-handler` (parser extrai `phoneNumberId`+`to` e dispara a pausa), `scripts/smoke-echo-pause-race.ts` (write-through antes da rede) e `smoke:debounce-flush` (pausa pré-brain/pós-brain e fallback suprimido).

> Contraparte no Receps: model `ConversationPause` + `BotConfig.botPausedUntil`/`echoPauseMinutes` + os 2 endpoints `pause-state`/`pause-conversation`. Ver AGENTS.md do Receps ("Pausa da Ana — contrato"). **Requer os endpoints no Receps de PRODUÇÃO** (mesma VPS, `localhost:3000`) — sem eles, a Ana cai no fail-open (responde normal).

### FIX 5 — Automações (lado Ana): escuta enquanto pausada + endpoint de atividade (2026-06-28)
Dois entregáveis do lado Ana da feature de automações (spec no Receps: `docs/spec-automacoes-mensagens-canais.md`, §8.1/§8.2/§11/INV-10). Ambos **ADITIVOS** e **à prova de falha**. NUNCA logam conteúdo/telefone do cliente (só `phoneNumberId` nas tags Sentry).

**A) Escuta enquanto pausada (§8.2/INV-10)** — antes a Ana DESCARTAVA tudo enquanto pausada (cega ao retomar → repetia pergunta / contradizia o humano). Agora **não responde, mas REGISTRA**:
- **Inbound do cliente** (`src/messageHandler.ts`): no ramo de pausa, ANTES do `return`, chama `recordInboundWhilePaused(conversationKey, text, config)` → grava role `user` (sem chamar o modelo, sem responder). Helper exportado + **injetável** (`PausedRecordDeps`, pro smoke), à prova de falha (erro de persistência → Sentry, NÃO propaga — a pausa não pode quebrar por isso). Não duplica: o caminho normal já grava o `user` dentro do `getReply`; o ramo de pausa não passa por lá.
- **Echo do humano** (`src/echoHandler.ts`): `handleSmbMessageEchoes` agora, além de pausar, **grava** cada echo como role `assistant` com prefixo **`[atendente] `** (`HUMAN_ECHO_PREFIX`, fonte única em `contextManager.ts`). Texto → o corpo; não-texto → placeholder curto (`[atendente] enviou uma imagem`). Parser PURO novo `parseEchoMessages` (exige `to` **e** `id`; NÃO deduplica por cliente — cada msg é gravada; `parseEchoTargets`/pausa segue intacto, sem exigir id). **Idempotência por message id** via `markEchoProcessed` (= `markMessageProcessed`, reusa a tabela `processed_messages`; ids de echo ≠ ids de inbound, sem colisão) → retransmissão da Meta não grava 2x. **Recuperação de blip de DB**: como a marca do id é gravada ANTES da gravação no histórico, se a gravação falhar o echo sumiria de vez na retransmissão (id já queimado) — por isso, na falha, `unmarkEcho` (= `unmarkMessageProcessed`) **desfaz a marca** pra a retransmissão re-tentar. Só desfaz na FALHA (no sucesso a marca fica) → **sem janela de gravação dupla**. `handleSmbMessageEchoes` **NUNCA lança**.
- **System prompt** (`src/services/brainService.ts`): linha nova "MENSAGENS DE ATENDENTE HUMANO" ensinando que mensagens começando com `[atendente] ` foram enviadas por um ATENDENTE HUMANO (não pela Ana) — usar como contexto, NÃO dizer "como eu te falei", NÃO repetir o que o humano já respondeu.
- Sinergia com §8.1: gravar o echo como `assistant` deixa o sinal "inbound sem resposta" correto (conversa respondida pelo humano = cliente NÃO está esperando).

**B) Endpoint `GET /internal/conversation-activity?phoneNumberId=&customerPhone=` (§8.1/§11)** — o motor de automação do Receps consulta ANTES de cada envio proativo (guarda de "bom momento"). **Auth**: `Authorization: Bearer <ERP_API_TOKEN>` (segredo compartilhado = `AI_BOT_API_KEY` do Receps; `isValidBearerToken` em `security.ts`, comparação **timing-safe**; sem token/inválido → **401**, nunca ecoa o token). Resposta `{ ultimoInboundSemResposta, ultimaAtividadeEm, fluxoAtivo }` — **NÃO** retorna `paused` (o Receps já tem isso no banco dele). Derivado do `ana_conversation_history`:
  - `getLastMessageMeta(phoneNumberId, customerPhone)` (`contextManager.ts`): última linha por `createdAt DESC, id DESC` = papel da última msg + `max(createdAt)` em 1 query. **Tolerante ao formato do telefone** via `customerPhoneVariants` (cru + só-dígitos → casa o `+5511…` canônico do Receps com o wa_id `5511…` gravado pela Ana; lookups por igualdade `= ANY($1)`, index-friendly, sem full scan).
  - `decideConversationActivity(input, now)` (`src/services/conversationActivity.ts`, **PURO**): `ultimoInboundSemResposta` = última msg é role `user`; `ultimaAtividadeEm` = ISO do max(createdAt); `fluxoAtivo` = atividade nos últimos **~5 min** (`FLUXO_ATIVO_WINDOW_MS`, **heurística** best-effort — não é detecção fina de "Ana em meio a um fluxo"). Sem histórico → `{false, null, false}`.
  - Params ausentes → 400; erro de DB → 500 (Sentry, sem PII). A rota NÃO passa por rate-limit/HMAC (esses middlewares só agem em POST `/webhook`).

**Smokes**: `smoke:listen-while-paused` (26 checks: inbound→`user`, à prova de falha, echo texto/imagem→`assistant` `[atendente] `, dedup por id, **recuperação de blip sem gravação dupla**, parser) e `smoke:conversation-activity` (17 checks: decisão pura + `customerPhoneVariants`). `smoke:echo-handler` atualizado (deps novas + `DATABASE_URL` dummy, já que o `echoHandler` agora importa o `contextManager`).

> Contraparte no Receps (paralelo): o motor de automação consome este endpoint + o estado de pausa do próprio banco do Receps. Ver spec §8.1/§11.

### FIX 6 — Histórico de conversas p/ o painel interno do Receps (read-only) (2026-07-23)
Dois endpoints GET internos que expõem o `ana_conversation_history` para o painel do dono do Receps (`/painel-receps`, SUPER_ADMIN acompanha a qualidade do atendimento da Ana por tenant). **100% READ-ONLY**: leem a janela rolante de `MAX_MESSAGES` (30) por conversa que a Ana já mantém — **NÃO tocam no caminho de escrita nem no trim** (`addMessage`/`getHistory` intactos). Mesmo molde do `/internal/conversation-activity`: auth **Bearer ERP_API_TOKEN** (`isValidBearerToken`, timing-safe; sem/errado → **401**, nunca ecoa o token); **NÃO** passam por rate-limit/HMAC (só o POST `/webhook` passa); erro de DB → **500** + Sentry com tags técnicas (`service`/`operation`/`phoneNumberId` — **sem** telefone/conteúdo do cliente). Bônus: o tenant `receps-vendas` (Renata) fica visível pelo mesmo mecanismo, sem trabalho extra.
- **`GET /internal/conversations?phoneNumberId=&limit=&offset=`** — lista as conversas do tenant. `phoneNumberId` obrigatório (senão 400); `limit` default 20 / máx 50; `offset` default 0. `200 → { conversations: [{ customerPhone, lastActivityAt (ISO), lastRole: "user"|"assistant", messageCount (≤30), lastPreview (≤120 chars) }], total }`. Agrega por `conversationKey` com prefixo `${phoneNumberId}:` (LIKE de prefixo escapado + `ESCAPE '\'`), ordena por última atividade DESC; `customerPhone` = sufixo após o 1º `:`.
- **`GET /internal/conversation-messages?phoneNumberId=&customerPhone=`** — thread de UMA conversa. Ambos obrigatórios (senão 400); tolerante ao formato do telefone (`customerPhoneVariants`). `200 → { messages: [{ role, content, createdAt (ISO) }] }` em ordem ASC (mais antiga primeiro); conversa inexistente → `{ messages: [] }` (200, não 404). Mensagens `assistant` com prefixo **`[atendente] `** (`HUMAN_ECHO_PREFIX`, echo do humano) VÃO no payload como estão — quem interpreta o marcador é a UI do Receps.
- **Leitores** (`src/services/contextManager.ts`, exportados): `listConversations`/`getHistoryWithTimestamps` (com query INJETÁVEL p/ o smoke) + puros `parseConversationKey`/`escapeLikePattern`/`truncatePreview`/`clampConversationsLimit`/`clampConversationsOffset`. **Smoke**: `smoke:conversations-endpoint` (puros + shape com pool mockado). NUNCA logam PII.

> Contraparte no Receps: `src/services/ana-conversations-admin.service.ts` consome via `RECEPS_IA_INTERNAL_API_URL` (com alias legado temporário) + UI em `/painel-receps/clientes/[tenantId]/conversas`. Ver AGENTS.md do Receps + `docs/features/plataforma.md`.

### FEATURE — Voz da Renata (sales-only, áudio-primeiro) (2026-07-23)
A Renata (`botRole === "sales"`) pode responder em nota de voz PTT pelo pipeline batch em `src/voice/`. O gate ÚNICO é `isRenataVoiceEnabled(config)`: exige sales + `RENATA_VOICE_ENABLED=true` + chave do provider selecionado + ffmpeg disponível. A flag é **OFF por default**; gate falso mantém o envio de texto anterior. A recepcionista nunca entra no pipeline.

- **TTS plugável:** default `elevenlabs` (`selfhosted` é stub); `gemini` é opt-in por `RENATA_TTS_PROVIDER`. ElevenLabs usa voz Carla `x8FWrDHAK5xiFTJLpnHq`, modelo obrigatório `eleven_multilingual_v2`, stability 0.40, similarity 0.75, speed 1.13, style 0.10. Gemini usa `gemini-3.1-flash-tts-preview`/Achernar, temperature 1.1 e direção padrão `Style: Vocal Smile. Pace: Natural.` — sem accent forçado; o ajuste de 2026-08-11 substituiu o antigo ritmo rápido, que exagerava a interpretação. O provider devolve PCM s16le com rate por resposta. O áudio inteiro é sintetizado e `audioEncoder.ts` converte por stdin/stdout para OGG/Opus 24 kbit/s, 24 kHz, mono, sem `/tmp`.
- **Probe Gemini real (2026-07-30):** resposta paga veio com `serviceTier:"standard"`, `audio/l16; rate=24000; channels=1` e `usageMetadata` com 63 tokens de áudio para 1,9665 s (~32 tokens/s; a regra de bolso de 25 tokens/s não bateu). O provider é stateless e devolve rate/uso na própria síntese; `tts_daily_usage.audio_tokens` guarda o consumo faturável real, enquanto duração por chars só é fallback quando o metadata não vem.
- **Política determinística:** `channelPolicy.ts` aplica áudio por padrão. Vira texto com opt-out persistido, e-mail, lista de horários, mensagem > `RENATA_VOICE_MAX_CHARS`, só-link ou fallback de sistema. Fala+link vira voz primeiro e texto contendo só o link depois. `channelPref.ts` detecta "manda texto"/"manda áudio" e persiste a preferência reversível por `conversationKey`.
- **Nunca silêncio:** `voiceDelivery.ts` captura falha de cache/TTS/ffmpeg/upload/send sem anexar o erro cru (Axios pode conter PII) e tenta `sendFreeformMessage` com o texto ORIGINAL, pré-prosódia. Fallback de flush/transcrição força texto e não passa pelo pipeline.
- **Cache/custo raw pg:** `tts_cache` usa SHA-256 do texto pós-prosódia + fingerprint do provider/voz; media ID fresco (<25d) evita TTS e upload, media velho reusa `ogg_bytes` e só reenvia o upload. `tts_daily_usage` separa por provider, soma chars e, no Gemini, `audio_tokens` reais + `audio_seconds` derivados dos tokens apenas em MISS; hits/misses e alerta Sentry uma vez/dia em 80% do orçamento. `renata_channel_prefs` guarda opt-out. As três tabelas são criadas no `boot()`.
- **Aberturas CTWA:** `salesOpeners.ts` casa os 2 textos reais dos anúncios no primeiro inbound, escolhe um script por hash estável do telefone, grava histórico/emite `primeira_resposta` e não chama Anthropic. Os scripts canônicos vivem em `OPENER_SCRIPTS`.
- **Saídas cobertas:** resposta reativa (`messageHandler`), régua (`salesFollowups`) e aviso de trial (`salesNotify`) usam o mesmo orquestrador/cache quando o gate está ligado. Logs de sales não imprimem telefone inteiro, nome nem conteúdo.
- **Smokes (não usam rede/DB/ffmpeg):** `smoke:renata-gemini-tts`, `smoke:renata-voice-policy`, `smoke:renata-voice-delivery`, `smoke:renata-voice-whatsapp`, `smoke:renata-voice-storage`, `smoke:renata-sales-openers`.

Deploy exige `ffmpeg -version` funcional na VPS. Se faltar, instalar antes de ligar a flag; o boot reporta `ffmpeg_missing` no Sentry em produção e mantém voz desabilitada.
